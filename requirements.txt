Flask>=2.3,<4
# TensorFlow 2.10 is the last version with Windows wheels; requires NumPy < 1.24
numpy==1.23.5
pandas==1.5.3
nltk>=3.8
tensorflow==2.10.0
# Keras aligned with TF 2.10 to support 'from keras import ...' imports
keras==2.10.0